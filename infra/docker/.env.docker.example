# docker-compose.full.yml 用的环境变量
# 复制本文件为 .env.docker.local 并填入真实值
# .env.docker.local 已被 .gitignore 忽略

# LLM
ANTHROPIC_API_KEY=sk-ant-xxx
OPENAI_API_KEY=
GOOGLE_GEMINI_API_KEY=

# Web Push（可选 · 留空走 stub）
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:noreply@loverush.com
