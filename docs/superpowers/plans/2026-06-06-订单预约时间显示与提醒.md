# 订单预约时间显示与两档提醒 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让进行中订单的列表与详情页（客户端+技师端）显示预约时间，并在预约前 1 小时 / 10 分钟两档主动提醒客户和技师（10 分钟档附带技师分身暖意消息）。

**Architecture:** 两部分。Part 1 纯前端——把 `OrderCard.tsx` 现成的时间工具抽成共享模块 `apps/web/lib/appointment-time.ts`，接入 4 个订单页（API 已返回 `scheduledAt`，不动后端）。Part 2 后端——orders 表加两个去重列，新建 `appointment-reminder` cron 每 2 分钟扫 `LOCKED/PAID` 未开始订单，按 `realEpoch()` 还原真实时刻判档，调 `notifications.enqueue()` 推送 + 10 分钟档调 `proactiveReachOut()` 触发分身消息。

**Tech Stack:** TypeScript / Next.js（apps/web）/ Hono + drizzle-orm（apps/api）/ PostgreSQL / vitest / Web Push。

---

## 文件结构

**Part 1 — 前端（apps/web）**
- 新建 `apps/web/lib/appointment-time.ts` —— 纯函数时间工具（apptLocalDate / absLabel / countdownLabel + pad / WEEK），单一真相源。
- 新建 `apps/web/lib/appointment-time.test.ts` —— 工具单测。
- 改 `apps/web/components/chat/OrderCard.tsx` —— 删本地副本，改 import 共享工具。
- 改 `apps/web/app/order/page.tsx` —— 客户订单列表卡片加预约时间行。
- 改 `apps/web/app/t/orders/page.tsx` —— 技师订单列表卡片加预约时间行。
- 改 `apps/web/app/order/[id]/page.tsx` —— 客户订单详情加绝对时间+倒计时块。
- 改 `apps/web/app/t/orders/[id]/page.tsx` —— 技师订单详情加绝对时间+倒计时块。

**Part 2 — 后端（apps/api + packages/db）**
- 新建迁移 `packages/db/migrations/0043_appointment_reminder.sql` —— orders 加两列。
- 改 `packages/db/src/schema/orders.ts` —— 加两个 timestamp 字段。
- 改 `apps/api/src/services/notifications.ts` —— Category 加 `appointment_reminder`。
- 新建 `apps/api/src/jobs/appointment-reminder.ts` —— cron 主体（含纯函数 `classifyReminder`）。
- 新建 `apps/api/src/jobs/appointment-reminder.test.ts` —— `classifyReminder` 边界单测。
- 新建 `apps/api/test/appointment-reminder.e2e.test.ts` —— cron 真库集成测。
- 改 `apps/api/src/jobs/index.ts` —— 注册 cron。

**实施顺序**：Part 1 先做（独立可测、零风险、能立即看到效果），Part 2 后做（迁移先上生产再 push）。

---

## Part 1 — 前端显示预约时间

### Task 1: 抽共享时间工具 + 单测

**Files:**
- Create: `apps/web/lib/appointment-time.ts`
- Test: `apps/web/lib/appointment-time.test.ts`

- [ ] **Step 1: 写工具文件**（逐字搬自 `OrderCard.tsx:37,61-96`，无改动）

```typescript
// apps/web/lib/appointment-time.ts
// 订单预约时间工具 · 单一真相源（原在 components/chat/OrderCard.tsx）
// UTC 墙上分量当本地 → 同城精确显示（v1 按 UTC 存、按本地墙上时间显示）

const WEEK = ['日', '一', '二', '三', '四', '五', '六'];
const pad = (n: number) => String(n).padStart(2, '0');

/** UTC 墙上分量当本地 → 真实本地 Date（同城精确） */
export function apptLocalDate(iso: string): Date {
  const d = new Date(iso);
  return new Date(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate(), d.getUTCHours(), d.getUTCMinutes());
}

/** 绝对时间标签：今天/明天/后天 HH:MM、周X HH:MM、M月D日 HH:MM */
export function absLabel(appt: Date): string {
  const now = new Date();
  const today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
  const day = new Date(appt.getFullYear(), appt.getMonth(), appt.getDate());
  const diff = Math.round((day.getTime() - today.getTime()) / 86_400_000);
  const time = `${pad(appt.getHours())}:${pad(appt.getMinutes())}`;
  if (diff === 0) return `今天 ${time}`;
  if (diff === 1) return `明天 ${time}`;
  if (diff === 2) return `后天 ${time}`;
  if (diff > 2 && diff < 7) return `周${WEEK[appt.getDay()]} ${time}`;
  return `${appt.getMonth() + 1}月${appt.getDate()}日 ${time}`;
}

/** 期待型分段倒计时：靠近、暖、不焦虑、不全程跳秒；已过点返回 null */
export function countdownLabel(appt: Date, now: number, therapistName: string): string | null {
  const ms = appt.getTime() - now;
  if (ms <= 0) return null;
  const min = Math.floor(ms / 60_000);
  if (min < 30) return `马上就能见到${therapistName}啦 ✨`;
  if (min < 180) {
    const h = Math.floor(min / 60);
    const m = min % 60;
    return `还有 ${h} 小时${m ? ` ${m} 分` : ''}就见面`;
  }
  const day = new Date(appt.getFullYear(), appt.getMonth(), appt.getDate());
  const t = new Date(now);
  const today = new Date(t.getFullYear(), t.getMonth(), t.getDate());
  if (day.getTime() === today.getTime()) return `就在今天 · 期待和你见面`;
  const days = Math.ceil(ms / 86_400_000);
  return `还有 ${days} 天就见到${therapistName}`;
}
```

- [ ] **Step 2: 写失败测试**

```typescript
// apps/web/lib/appointment-time.test.ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { apptLocalDate, absLabel, countdownLabel } from './appointment-time';

describe('apptLocalDate', () => {
  it('把 UTC 墙上分量当本地分量构造', () => {
    const d = apptLocalDate('2026-06-10T14:30:00.000Z');
    expect(d.getHours()).toBe(14);
    expect(d.getMinutes()).toBe(30);
    expect(d.getDate()).toBe(10);
    expect(d.getMonth()).toBe(5); // 6月=5
  });
});

describe('absLabel', () => {
  beforeEach(() => {
    vi.useFakeTimers();
    vi.setSystemTime(new Date(2026, 5, 10, 9, 0, 0)); // 2026-06-10 09:00 本地
  });
  afterEach(() => vi.useRealTimers());

  it('今天', () => {
    expect(absLabel(new Date(2026, 5, 10, 14, 5))).toBe('今天 14:05');
  });
  it('明天', () => {
    expect(absLabel(new Date(2026, 5, 11, 9, 0))).toBe('明天 09:00');
  });
  it('后天', () => {
    expect(absLabel(new Date(2026, 5, 12, 18, 30))).toBe('后天 18:30');
  });
  it('本周内（>2 <7 天）显示周X', () => {
    // 2026-06-13 是周六
    expect(absLabel(new Date(2026, 5, 13, 10, 0))).toBe('周六 10:00');
  });
  it('超 7 天显示月日', () => {
    expect(absLabel(new Date(2026, 5, 20, 10, 0))).toBe('6月20日 10:00');
  });
});

describe('countdownLabel', () => {
  const name = '小雅';
  it('已过点返回 null', () => {
    const appt = new Date(2026, 5, 10, 9, 0);
    expect(countdownLabel(appt, appt.getTime() + 60_000, name)).toBeNull();
  });
  it('30 分钟内：马上见面', () => {
    const appt = new Date(2026, 5, 10, 9, 20);
    const now = new Date(2026, 5, 10, 9, 0).getTime(); // 还剩 20 分
    expect(countdownLabel(appt, now, name)).toBe(`马上就能见到${name}啦 ✨`);
  });
  it('30~180 分钟：还有 N 小时 M 分', () => {
    const appt = new Date(2026, 5, 10, 11, 30);
    const now = new Date(2026, 5, 10, 9, 0).getTime(); // 还剩 2h30m
    expect(countdownLabel(appt, now, name)).toBe('还有 2 小时 30 分就见面');
  });
  it('当天但 >180 分钟：就在今天', () => {
    const appt = new Date(2026, 5, 10, 18, 0);
    const now = new Date(2026, 5, 10, 9, 0).getTime(); // 还剩 9h，同一天
    expect(countdownLabel(appt, now, name)).toBe('就在今天 · 期待和你见面');
  });
  it('跨天：还有 N 天', () => {
    const appt = new Date(2026, 5, 13, 10, 0);
    const now = new Date(2026, 5, 10, 9, 0).getTime();
    expect(countdownLabel(appt, now, name)).toBe(`还有 3 天就见到${name}`);
  });
});
```

- [ ] **Step 3: 运行测试确认通过**（工具与测试同时写好，应直接通过）

Run: `cd apps/web && pnpm exec vitest run lib/appointment-time.test.ts`
Expected: PASS（全部用例绿）

- [ ] **Step 4: 提交**

```bash
git add apps/web/lib/appointment-time.ts apps/web/lib/appointment-time.test.ts
git commit -m "feat(web): 抽订单预约时间工具 appointment-time + 单测"
```

---

### Task 2: OrderCard 改用共享工具（消除重复）

**Files:**
- Modify: `apps/web/components/chat/OrderCard.tsx`（删 L37 WEEK、L61-62 pad、L63-96 三函数；加 import）

- [ ] **Step 1: 加 import**

在 `OrderCard.tsx` 顶部 import 区加：

```typescript
import { apptLocalDate, absLabel, countdownLabel } from '../../lib/appointment-time';
```

> 注：相对路径按文件实际层级调整（`components/chat/` → `lib/`，即 `../../lib/appointment-time`）。若项目配了 `@/` 别名，用 `@/lib/appointment-time`，与同文件其它 import 风格一致。

- [ ] **Step 2: 删除本地副本**

删掉 `OrderCard.tsx` 内的本地定义：`const WEEK = [...]`（L37）、`const pad = ...`（L61）、`function apptLocalDate`（L63-66）、`function absLabel`（L68-78）、`function countdownLabel`（L80-96）。保留其余逻辑不变（调用处签名一致，无需改）。

> 若 `WEEK`/`pad` 在该文件别处仍被引用，保留这两个本地常量、只删三个函数即可。先 grep 确认：`grep -n 'WEEK\|pad(' apps/web/components/chat/OrderCard.tsx`。

- [ ] **Step 3: typecheck**

Run: `cd apps/web && pnpm exec tsc --noEmit`
Expected: 无新增错误（与改动文件相关的报错为 0）

- [ ] **Step 4: 提交**

```bash
git add apps/web/components/chat/OrderCard.tsx
git commit -m "refactor(web): OrderCard 改用共享 appointment-time 工具"
```

---

### Task 3: 客户订单列表显示预约时间

**Files:**
- Modify: `apps/web/app/order/page.tsx`（卡片渲染 L135-209，在心动金状态行 L206 之后插入）

- [ ] **Step 1: 确认列表项类型含 scheduledAt**

Run: `grep -n 'scheduledAt' apps/web/app/order/page.tsx`
若类型里没有 `scheduledAt`，在该页订单项的 type/interface 上补 `scheduledAt?: string | null;`（listOrders 已返回该字段，仅前端类型需声明）。

- [ ] **Step 2: 加 import**

```typescript
import { apptLocalDate, absLabel } from '../../lib/appointment-time';
```

> 路径按实际层级与 `@/` 别名约定调整（与本文件其它 import 一致）。

- [ ] **Step 3: 卡片内插入预约时间行**

在每张卡片心动金状态块（约 L206）之后插入（列表只放绝对时间，不放倒计时，避免列表焦虑感）：

```tsx
{o.scheduledAt && (
  <div className="mt-1 flex items-center gap-1 text-[13px] text-rose-600">
    <span>📅</span>
    <span>预约 {absLabel(apptLocalDate(o.scheduledAt))}</span>
  </div>
)}
```

> className 仅为示意——实现时套用该文件既有的卡片文字样式（颜色/字号/间距与同卡片其它行一致），不要引入新风格。

- [ ] **Step 4: 跑起来肉眼验证**

Run: 启动本地前端（见 `STARTUP-GUIDE.md` / 本地 QA 栈：pg 54399 / api 8787 / web 4321），用种子码登录客户，进 `/order` 看有预约时间的订单卡是否显示「预约 明天 HH:MM」。
Expected: 有 `scheduledAt` 的订单卡显示预约时间行，无 `scheduledAt` 的不显示。

- [ ] **Step 5: 提交**

```bash
git add apps/web/app/order/page.tsx
git commit -m "feat(web): 客户订单列表卡片显示预约时间"
```

---

### Task 4: 技师订单列表显示预约时间

**Files:**
- Modify: `apps/web/app/t/orders/page.tsx`

- [ ] **Step 1: 确认类型 + 加 import**

同 Task 3 Step 1-2：grep `scheduledAt`，类型补字段，import `apptLocalDate, absLabel`（路径按 `app/t/orders/` → `lib/`，即 `../../../lib/appointment-time` 或 `@/lib/...`）。

- [ ] **Step 2: 卡片内插入预约时间行**

在技师订单卡片信息块末尾插入（技师视角文案用「客户预约」）：

```tsx
{o.scheduledAt && (
  <div className="mt-1 flex items-center gap-1 text-[13px] text-rose-600">
    <span>📅</span>
    <span>预约 {absLabel(apptLocalDate(o.scheduledAt))}</span>
  </div>
)}
```

> className 套用本文件既有卡片样式。

- [ ] **Step 3: 肉眼验证**

Run: 本地登录技师（verification_status='passed' 的种子技师），进 `/t/orders` 看卡片显示预约时间。
Expected: 有预约时间的订单卡显示。

- [ ] **Step 4: 提交**

```bash
git add apps/web/app/t/orders/page.tsx
git commit -m "feat(web): 技师订单列表卡片显示预约时间"
```

---

### Task 5: 客户订单详情显示绝对时间 + 倒计时块

**Files:**
- Modify: `apps/web/app/order/[id]/page.tsx`（大状态卡 L168-209 内插入；已有 scheduledAt 计算在 L157-163）

- [ ] **Step 1: 加 import**

```typescript
import { apptLocalDate, absLabel, countdownLabel } from '../../../lib/appointment-time';
```

> 路径按 `app/order/[id]/` → `lib/`（`../../../lib/...`）或 `@/lib/...`。

- [ ] **Step 2: 加每分钟刷新的 now tick（让倒计时活的）**

在组件内（已有 hook 区，注意必须在所有 early return 之前——本项目踩过 React #310 hook 越位，见详情页历史）加：

```tsx
const [nowTick, setNowTick] = useState(() => Date.now());
useEffect(() => {
  const t = setInterval(() => setNowTick(Date.now()), 60_000);
  return () => clearInterval(t);
}, []);
```

> 若文件顶部未引入 `useState`/`useEffect`，在 React import 处补上。

- [ ] **Step 3: 派生预约时间展示值**

在已有 `scheduledMs`（L157）附近加（同样放在 early return 之前）：

```tsx
const apptDate = order?.scheduledAt ? apptLocalDate(order.scheduledAt) : null;
const apptAbs = apptDate ? absLabel(apptDate) : null;
const apptCountdown = apptDate ? countdownLabel(apptDate, nowTick, order?.therapist?.name ?? '技师') : null;
```

> `order?.therapist?.name` 按该页 order 详情类型里技师名的真实字段调整（grep `therapist` 确认，getOrderDetail 返回 `therapist` 对象）。

- [ ] **Step 4: 在大状态卡内渲染预约时间块**

在大状态卡（L168-209）合适位置（状态徽章下方）插入。临近时（倒计时存在且为「马上/还有 N 小时」）升暖色仪式卡：

```tsx
{apptAbs && (
  <div className="mt-3 rounded-xl bg-rose-50 px-4 py-3">
    <div className="flex items-center gap-2 text-rose-700">
      <span>📅</span>
      <span className="font-medium">预约时间 {apptAbs}</span>
    </div>
    {apptCountdown && (
      <div className="mt-1 text-[13px] text-rose-500">{apptCountdown}</div>
    )}
  </div>
)}
```

> className 套用该页既有卡片样式系统，不引入新视觉风格。

- [ ] **Step 5: 肉眼验证**

Run: 本地登录客户进某个有 scheduledAt 的订单详情 `/order/<id>`。
Expected: 大状态卡显示「预约时间 明天 HH:MM」+ 下方期待型倒计时文案；倒计时随时间推移更新（可改本地系统时间或等 1 分钟验证 tick）。

- [ ] **Step 6: 提交**

```bash
git add 'apps/web/app/order/[id]/page.tsx'
git commit -m "feat(web): 客户订单详情显示预约时间+期待倒计时"
```

> 注：路径含 `[id]`，commit 时若遇 pathspec 问题加 `GIT_LITERAL_PATHSPECS=1`（本项目已知坑）。

---

### Task 6: 技师订单详情显示绝对时间 + 倒计时块

**Files:**
- Modify: `apps/web/app/t/orders/[id]/page.tsx`

- [ ] **Step 1: 加 import + now tick + 派生值**

照 Task 5 Step 1-3，路径按 `app/t/orders/[id]/` → `lib/`（`../../../../lib/...`）或 `@/lib/...`。倒计时人名用客户名（技师视角）：

```tsx
const apptCountdown = apptDate ? countdownLabel(apptDate, nowTick, order?.customer?.name ?? '客户') : null;
```

> `order?.customer?.name` 按技师端详情类型真实字段调整（grep 确认）。hook 一律放 early return 之前。

- [ ] **Step 2: 渲染预约时间块**

照 Task 5 Step 4，文案技师视角（如「与客户的预约 {apptAbs}」），className 套用技师端详情页既有样式。

- [ ] **Step 3: 肉眼验证**

Run: 本地登录技师进 `/t/orders/<id>`。
Expected: 详情显示预约时间 + 倒计时。

- [ ] **Step 4: 提交**

```bash
GIT_LITERAL_PATHSPECS=1 git add 'apps/web/app/t/orders/[id]/page.tsx'
git commit -m "feat(web): 技师订单详情显示预约时间+期待倒计时"
```

---

## Part 2 — 两档预约提醒 cron

> ⚠️ 上线铁律：迁移**先在生产跑**再 push 代码（防部署窗口报列不存在）。本地用 `loverush_test` 跑 `drizzle-kit push` 或手动执行迁移 SQL。

### Task 7: 迁移加去重列 + schema 字段

**Files:**
- Create: `packages/db/migrations/0043_appointment_reminder.sql`
- Modify: `packages/db/src/schema/orders.ts`（在 `offlinePaidAt` 字段之后加两列）

- [ ] **Step 1: 确认迁移目录与下一个编号**

Run: `ls packages/db/migrations/ | sort | tail -3`
Expected: 最大为 `0042_customer_ai_event.sql`，下一个为 `0043`。若目录路径不同（如 `packages/db/drizzle/`），用实际路径。

- [ ] **Step 2: 写迁移 SQL**

```sql
-- packages/db/migrations/0043_appointment_reminder.sql
-- 0043 · 预约前两档提醒 · 去重标记列
-- 来源:packages/db/src/schema/orders.ts(reminder1hSentAt / reminder10mSentAt)
-- 非破坏 · 可空加列 · 幂等 ADD COLUMN IF NOT EXISTS
-- 上线顺序:先在生产跑本迁移,再 push 代码(防部署窗口报列不存在)。
-- 用途:appointment-reminder cron 防同一订单同一档位重复推送。

ALTER TABLE orders ADD COLUMN IF NOT EXISTS reminder_1h_sent_at timestamp with time zone;
ALTER TABLE orders ADD COLUMN IF NOT EXISTS reminder_10m_sent_at timestamp with time zone;
```

- [ ] **Step 3: schema 加字段**

在 `packages/db/src/schema/orders.ts` 的 `offlinePaidAt: timestamp('offline_paid_at', { withTimezone: true }),` 之后加：

```typescript
reminder1hSentAt: timestamp('reminder_1h_sent_at', { withTimezone: true }),
reminder10mSentAt: timestamp('reminder_10m_sent_at', { withTimezone: true }),
```

- [ ] **Step 4: 本地测试库应用迁移**

Run: `cd packages/db && DATABASE_URL=<loverush_test 串> pnpm exec drizzle-kit push`（或手动 `psql "$DATABASE_URL" -f migrations/0043_appointment_reminder.sql`）
Expected: 两列建成，无报错。验证：`psql "$DATABASE_URL" -c '\d orders' | grep reminder`

- [ ] **Step 5: typecheck**

Run: `cd packages/db && pnpm exec tsc --noEmit`
Expected: 无新增错误。

- [ ] **Step 6: 提交**

```bash
git add packages/db/migrations/0043_appointment_reminder.sql packages/db/src/schema/orders.ts
git commit -m "feat(db): orders 加预约提醒去重列 reminder_1h/10m_sent_at (0043)"
```

---

### Task 8: notifications 加 appointment_reminder 类目

**Files:**
- Modify: `apps/api/src/services/notifications.ts`（Category union L28-35；CATEGORY_PREF_KEY L52-60）

- [ ] **Step 1: 扩 Category union**

把 `appointment_reminder` 加进 `Category`：

```typescript
export type Category =
  | 'chat_msg'
  | 'order_status'
  | 'dispatch_offer'
  | 'review'
  | 'withdraw'
  | 'system'
  | 'promo'
  | 'appointment_reminder';
```

- [ ] **Step 2: 扩 CATEGORY_PREF_KEY（复用 orderStatus 开关，不新增偏好列）**

在 `CATEGORY_PREF_KEY` 映射里加（提醒属订单相关，复用 `orderStatusEnabled`，免改 userPushPreferences 表）：

```typescript
const CATEGORY_PREF_KEY: Record<Category, keyof UserPushPreference> = {
  chat_msg: 'chatMsgEnabled',
  order_status: 'orderStatusEnabled',
  dispatch_offer: 'dispatchOfferEnabled',
  review: 'reviewEnabled',
  withdraw: 'withdrawEnabled',
  system: 'orderStatusEnabled',
  promo: 'promoEnabled',
  appointment_reminder: 'orderStatusEnabled', // 提醒属订单相关，复用订单开关
};
```

- [ ] **Step 3: typecheck**

Run: `cd apps/api && pnpm exec tsc --noEmit`
Expected: 无新增错误（Record 类型要求每个 Category 都有映射，漏了会报错——确保两处都加）。

- [ ] **Step 4: 提交**

```bash
git add apps/api/src/services/notifications.ts
git commit -m "feat(api): notifications 加 appointment_reminder 类目"
```

---

### Task 9: appointment-reminder cron（含 classifyReminder 纯函数）

**Files:**
- Create: `apps/api/src/jobs/appointment-reminder.ts`
- Test: `apps/api/src/jobs/appointment-reminder.test.ts`（classifyReminder 边界单测）

- [ ] **Step 1: 先写 classifyReminder 的失败测试**

```typescript
// apps/api/src/jobs/appointment-reminder.test.ts
import { describe, it, expect } from 'vitest';
import { classifyReminder } from './appointment-reminder';

// classifyReminder(minutesUntil, sent1h, sent10m) → { fire1h: boolean; fire10m: boolean }
describe('classifyReminder', () => {
  it('刚好 60 分：触发 1h 档', () => {
    expect(classifyReminder(60, false, false)).toEqual({ fire1h: true, fire10m: false });
  });
  it('61 分：还没到 1h 档', () => {
    expect(classifyReminder(61, false, false)).toEqual({ fire1h: false, fire10m: false });
  });
  it('30 分（1h 档窗口内、未发过）：补发 1h 档', () => {
    expect(classifyReminder(30, false, false)).toEqual({ fire1h: true, fire10m: false });
  });
  it('30 分但 1h 已发过：不重复', () => {
    expect(classifyReminder(30, true, false)).toEqual({ fire1h: false, fire10m: false });
  });
  it('刚好 10 分：触发 10m 档（不再触发 1h）', () => {
    expect(classifyReminder(10, false, false)).toEqual({ fire1h: false, fire10m: true });
  });
  it('5 分（10m 窗口内、未发过）：触发 10m', () => {
    expect(classifyReminder(5, true, false)).toEqual({ fire1h: false, fire10m: true });
  });
  it('5 分但 10m 已发过：不重复', () => {
    expect(classifyReminder(5, true, true)).toEqual({ fire1h: false, fire10m: false });
  });
  it('0 分及以下：都不触发（已到点/过点）', () => {
    expect(classifyReminder(0, false, false)).toEqual({ fire1h: false, fire10m: false });
    expect(classifyReminder(-5, false, false)).toEqual({ fire1h: false, fire10m: false });
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/api && pnpm exec vitest run src/jobs/appointment-reminder.test.ts`
Expected: FAIL（`classifyReminder` 未定义 / 模块不存在）

- [ ] **Step 3: 写 cron 文件（含 classifyReminder）**

```typescript
// apps/api/src/jobs/appointment-reminder.ts
/**
 * Job · 预约前两档提醒（1 小时 / 10 分钟）
 * 扫 LOCKED/PAID 未开始订单 → 按技师时区还原真实时刻判档 →
 *   1h 档:系统推送给客户+技师
 *   10m 档:系统推送 + 触发技师分身"快到啦"暖意消息
 * 去重:orders.reminder_1h_sent_at / reminder_10m_sent_at
 */

import { sql } from 'drizzle-orm';
import type { Database } from '@loverush/db';
import { enqueue } from '../services/notifications';
import { proactiveReachOut } from '../services/ai_alter';
import { realEpoch } from './ai-alter-pre-appointment';
import { resolveTz } from '../services/therapist_facts';
import { logger } from '../services/logger';

export interface JobContext {
  db: Database;
}

/** 纯函数:给定距预约真实分钟数 + 两档已发标记,判定本轮该触发哪些档 */
export function classifyReminder(
  minutesUntil: number,
  sent1h: boolean,
  sent10m: boolean,
): { fire1h: boolean; fire10m: boolean } {
  if (minutesUntil <= 0) return { fire1h: false, fire10m: false };
  // 10m 档优先:进入 10 分钟窗口只发 10m（不再补 1h，避免临门连发两条）
  if (minutesUntil <= 10) {
    return { fire1h: false, fire10m: !sent10m };
  }
  // 1h 档窗口:(10, 60]
  if (minutesUntil <= 60) {
    return { fire1h: !sent1h, fire10m: false };
  }
  return { fire1h: false, fire10m: false };
}

interface DueRow {
  id: string;
  order_no: string | null;
  customer_id: string;
  therapist_user_id: string;
  scheduled_at: string;
  service_country: string | null;
  service_city: string | null;
  reminder_1h_sent_at: string | null;
  reminder_10m_sent_at: string | null;
}

export async function runAppointmentReminder(
  ctx: JobContext,
): Promise<{ scanned: number; fired1h: number; fired10m: number }> {
  // 只扫已锁定时间、未开始、有预约时刻、未来 75 分钟内的单（窗口略宽于 60 防边界漏）
  const rows = (await ctx.db.execute(sql`
    SELECT id, order_no, customer_id, therapist_user_id, scheduled_at,
           service_country, service_city, reminder_1h_sent_at, reminder_10m_sent_at
    FROM orders
    WHERE status IN ('LOCKED','PAID')
      AND scheduled_at IS NOT NULL
      AND scheduled_at > now() - INTERVAL '15 minutes'
      AND scheduled_at < now() + INTERVAL '90 minutes'
    ORDER BY scheduled_at
    LIMIT 200
  `)) as unknown as DueRow[];

  let fired1h = 0;
  let fired10m = 0;
  const now = Date.now();

  for (const r of rows) {
    try {
      const tz = resolveTz({ serviceCountry: r.service_country, serviceCity: r.service_city });
      const realMs = realEpoch(r.scheduled_at, tz);
      const minutesUntil = (realMs - now) / 60_000;
      const { fire1h, fire10m } = classifyReminder(
        minutesUntil,
        r.reminder_1h_sent_at != null,
        r.reminder_10m_sent_at != null,
      );

      if (fire1h) {
        await enqueue({ db: ctx.db }, {
          recipientUserId: r.customer_id,
          level: 'important',
          category: 'appointment_reminder',
          title: '预约提醒',
          body: '您预约的服务还有约 1 小时开始，点击查看详情',
          deepLink: `/order/${r.id}`,
        });
        await enqueue({ db: ctx.db }, {
          recipientUserId: r.therapist_user_id,
          level: 'important',
          category: 'appointment_reminder',
          title: '预约提醒',
          body: '您有一个预约还有约 1 小时开始，点击查看详情',
          deepLink: `/t/orders/${r.id}`,
        });
        await ctx.db.execute(sql`UPDATE orders SET reminder_1h_sent_at = now() WHERE id = ${r.id}`);
        fired1h++;
      }

      if (fire10m) {
        await enqueue({ db: ctx.db }, {
          recipientUserId: r.customer_id,
          level: 'important',
          category: 'appointment_reminder',
          title: '马上见面啦',
          body: '您预约的服务还有约 10 分钟就开始，点击查看详情',
          deepLink: `/order/${r.id}`,
        });
        await enqueue({ db: ctx.db }, {
          recipientUserId: r.therapist_user_id,
          level: 'important',
          category: 'appointment_reminder',
          title: '马上见面啦',
          body: '您有一个预约还有约 10 分钟就开始，点击查看详情',
          deepLink: `/t/orders/${r.id}`,
        });
        // 分身暖意消息（临门一脚，与 2.5h 的"期待铺垫"语义区分）。LLM 不可用时容错跳过。
        try {
          await proactiveReachOut({ db: ctx.db }, {
            customerId: r.customer_id,
            therapistUserId: r.therapist_user_id,
            scenario: 'pre_appointment_10min',
            situationPrompt:
              `（内部触发·不是客户发来的消息）你和这位客户的预约马上就到了（大约十分钟内就见面）。` +
              `以你本人的身份,主动发一句"我已经准备好啦、马上见到你好期待"那种又暖又雀跃的话。` +
              `绝不提钱/不催/不报价/不加项/不推销,也不要重新承诺或改时间地点。短,像真人发微信。直接输出那一两句话。`,
          });
        } catch (err) {
          logger.error('appointment_reminder.alter_failed', { orderId: r.id, err: String(err) });
        }
        await ctx.db.execute(sql`UPDATE orders SET reminder_10m_sent_at = now() WHERE id = ${r.id}`);
        fired10m++;
      }
    } catch (err) {
      logger.error('appointment_reminder.row_failed', { orderId: r.id, err: String(err) });
    }
  }

  if (fired1h > 0 || fired10m > 0) {
    logger.info('appointment_reminder.done', { scanned: rows.length, fired1h, fired10m });
  }
  return { scanned: rows.length, fired1h, fired10m };
}

let timer: NodeJS.Timeout | null = null;

export function startAppointmentReminderCron(ctx: JobContext, intervalMs = 2 * 60 * 1000): void {
  if (timer) return;
  void runAppointmentReminder(ctx).catch((err) =>
    logger.error('appointment_reminder.bootstrap_failed', { err: String(err) }),
  );
  timer = setInterval(() => {
    runAppointmentReminder(ctx).catch((err) =>
      logger.error('appointment_reminder.tick_failed', { err: String(err) }),
    );
  }, intervalMs);
}
```

> 核实点（实现时逐一确认，不符就按真实签名调整）：
> - `enqueue` 第一参 `NotifyContext` 的真实形状——若不是 `{ db }`，按 `notifications.ts` 里 `NotifyContext` 定义传（grep 确认）。
> - `proactiveReachOut` 第一参 `AiAlterContext`——同上按真实形状传。
> - `realEpoch` 从 `ai-alter-pre-appointment.ts` 是否已 `export`（Explore 显示已 export）；`resolveTz` 从 `therapist_facts.ts` 已 export。
> - `service_country`/`service_city` 是 orders 表真实列名（grep `\d orders` 确认；若在别的表需调整 SQL）。

- [ ] **Step 4: 跑 classifyReminder 单测确认通过**

Run: `cd apps/api && pnpm exec vitest run src/jobs/appointment-reminder.test.ts`
Expected: PASS（8 个边界用例全绿）

- [ ] **Step 5: typecheck**

Run: `cd apps/api && pnpm exec tsc --noEmit`
Expected: 无新增错误。

- [ ] **Step 6: 提交**

```bash
git add apps/api/src/jobs/appointment-reminder.ts apps/api/src/jobs/appointment-reminder.test.ts
git commit -m "feat(api): 预约前两档提醒 cron + classifyReminder 单测"
```

---

### Task 10: 注册 cron + 真库集成测

**Files:**
- Modify: `apps/api/src/jobs/index.ts`（import + 在 `startAllAssistantJobs` 注册 + 导出 run 函数）
- Test: `apps/api/test/appointment-reminder.e2e.test.ts`

- [ ] **Step 1: 注册到 jobs/index.ts**

顶部 import：

```typescript
import { startAppointmentReminderCron, runAppointmentReminder } from './appointment-reminder';
```

在 `startAllAssistantJobs(ctx)` 函数体末尾加：

```typescript
  startAppointmentReminderCron(ctx); // 预约前 1h/10m 两档提醒
```

并在文件底部 `export { ... }` 列表里加 `runAppointmentReminder,`。

> 核实 `startAllAssistantJobs` 的 `ctx` 类型（`JobsContext`）是否含 `db`——cron 的 `JobContext` 只需 `{ db }`，若 `JobsContext` 字段更多，传 `ctx` 即可（结构兼容）。

- [ ] **Step 2: 写真库集成测（失败先行）**

```typescript
// apps/api/test/appointment-reminder.e2e.test.ts
/**
 * 预约前两档提醒 cron e2e
 * 主干:注册 customer+therapist → 建一单设 status=LOCKED + scheduled_at 在 1h 档窗口
 *   跑 cron → 客户+技师各收到 1 条 appointment_reminder 通知;reminder_1h_sent_at 落库
 *   再跑一次 → 不重复推（去重）
 * 跑:
 *   cd apps/api
 *   DATABASE_URL=<loverush_test> pnpm exec vitest run test/appointment-reminder.e2e.test.ts
 */
import { describe, it, expect, beforeAll } from 'vitest';
import { sql } from 'drizzle-orm';
import { runAppointmentReminder } from '../src/jobs/appointment-reminder';
import { getDb, registerNew, truncateAll } from './helpers';

describe('appointment-reminder cron', () => {
  const db = getDb();
  let customerId = '';
  let therapistUserId = '';
  let orderId = '';

  beforeAll(async () => {
    await truncateAll();
    const c = await registerNew('customer');
    const t = await registerNew('therapist');
    customerId = c.userId;
    therapistUserId = t.userId;

    // 直接插一单:状态 LOCKED + 预约时刻设在 ~45 分钟后（落在 1h 档窗口 (10,60]）
    // 注:orders 有若干 NOT NULL 列。先跑本测试,DB 报哪列缺就补哪列（用 schema 默认/最小值）。
    const rows = (await db.execute(sql`
      INSERT INTO orders (customer_id, therapist_user_id, status, scheduled_at, service_country, service_city)
      VALUES (${customerId}, ${therapistUserId}, 'LOCKED', now() + INTERVAL '45 minutes', 'TH', 'bangkok')
      RETURNING id
    `)) as unknown as Array<{ id: string }>;
    orderId = rows[0].id;
  });

  it('1h 档:客户+技师各收到提醒 + 标记落库', async () => {
    const res = await runAppointmentReminder({ db });
    expect(res.fired1h).toBeGreaterThanOrEqual(1);

    const notifs = (await db.execute(sql`
      SELECT recipient_user_id FROM notifications
      WHERE category = 'appointment_reminder'
        AND recipient_user_id IN (${customerId}, ${therapistUserId})
    `)) as unknown as Array<{ recipient_user_id: string }>;
    const recipients = new Set(notifs.map((n) => n.recipient_user_id));
    expect(recipients.has(customerId)).toBe(true);
    expect(recipients.has(therapistUserId)).toBe(true);

    const ord = (await db.execute(sql`
      SELECT reminder_1h_sent_at FROM orders WHERE id = ${orderId}
    `)) as unknown as Array<{ reminder_1h_sent_at: string | null }>;
    expect(ord[0].reminder_1h_sent_at).not.toBeNull();
  });

  it('二次扫:不重复推（去重）', async () => {
    const before = (await db.execute(sql`
      SELECT count(*)::int AS n FROM notifications WHERE category = 'appointment_reminder'
    `)) as unknown as Array<{ n: number }>;
    const res = await runAppointmentReminder({ db });
    expect(res.fired1h).toBe(0);
    const after = (await db.execute(sql`
      SELECT count(*)::int AS n FROM notifications WHERE category = 'appointment_reminder'
    `)) as unknown as Array<{ n: number }>;
    expect(after[0].n).toBe(before[0].n);
  });
});
```

> 真实列名核实：`notifications` 表的「收件人」「类目」列名以 schema 为准（grep `notifications` schema 确认 `recipient_user_id` / `category` 真实列名，不符就改 SQL）。INSERT orders 若报 NOT NULL 缺列，按 `\d orders` 补最小合法值（如 `service_mode`、价格列等）——这是真实执行调整，非占位。

- [ ] **Step 3: 跑集成测**

Run: `cd apps/api && DATABASE_URL=<loverush_test 串> pnpm exec vitest run test/appointment-reminder.e2e.test.ts`
Expected: PASS（两个用例绿）。首跑若 INSERT 报缺列，按报错补列后重跑。

- [ ] **Step 4: typecheck + lint**

Run: `cd apps/api && pnpm exec tsc --noEmit && pnpm exec eslint src/jobs/appointment-reminder.ts --max-warnings=0`
Expected: 无新增错误。

- [ ] **Step 5: 提交**

```bash
git add apps/api/src/jobs/index.ts apps/api/test/appointment-reminder.e2e.test.ts
git commit -m "feat(api): 注册预约提醒 cron + 真库集成测"
```

---

## 上线顺序（Part 2）

1. 先在**生产库**跑 `0043_appointment_reminder.sql`（`railway run -- bash -c 'psql "$DATABASE_URL" -f packages/db/migrations/0043_appointment_reminder.sql'`，注入子进程不外泄凭证）。
2. 确认生产 `\d orders` 有两列后，再 push 代码触发部署。
3. 部署后看 api 日志 `appointment_reminder.done` 是否按预期出现；造一单 scheduled_at 在 1h/10m 窗口验证客户+技师真收到推送。

---

## 自查（spec 覆盖核对）

| spec 需求 | 对应 Task |
|---|---|
| 列表显示预约时间（客户） | Task 3 |
| 列表显示预约时间（技师） | Task 4 |
| 详情显示预约时间（客户） | Task 5 |
| 详情显示预约时间（技师） | Task 6 |
| 绝对时间+期待型倒计时 | Task 5/6（countdownLabel） |
| 共享时间工具/消除重复 | Task 1/2 |
| 1h 档系统推送（客户+技师） | Task 9（classifyReminder + enqueue） |
| 10m 档系统推送+分身暖意 | Task 9（proactiveReachOut） |
| 只覆盖 LOCKED/PAID 未开始 | Task 9（SQL WHERE status IN） |
| 去重防重复推送 | Task 7（两列）+ Task 9（classifyReminder + UPDATE） |
| 时区还原真实时刻 | Task 9（realEpoch + resolveTz） |
| 与现有 2.5h 分身消息区分 | Task 9（scenario=pre_appointment_10min + 文案"马上见面"） |
| 新增 appointment_reminder 类目 | Task 8 |
| cron 注册 | Task 10 |
| 测试覆盖（工具/边界/真库） | Task 1/9/10 |
| 迁移先上生产再 push | 上线顺序节 |
