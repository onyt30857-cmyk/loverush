# 分身回复语音化（30% 语音 / 70% 文字）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** M06 技师分身回复客户时，约 30% 用技师本人复刻声音发语音、70% 文字；只对已真克隆声音的技师 + 短回复触发，失败静默降级文字。

**Architecture:** 后端单点接入 `ai_alter.ts` 的 `maybeReplyAsAlter` 发送段——在 `validateOutput` 后、防双发检查后、分段前加一道决策：纯函数短句门 + 30% 抽签（便宜，先算）→ 技师真克隆门（查库，最后算）。命中则整条 `finalText` 合成一条 `type:'voice'` 消息（不分段），失败回落原有多段文字路径。前端聊天页零改（`VoiceWhisperBubble` 已就绪），仅给技师端 `VoiceCloneCard` 加一张催复刻引导卡。

**Tech Stack:** TypeScript / Hono / Drizzle / Vitest（后端）；Next.js / React（前端）。复用 `synthesizeWhisper`（ElevenLabs 真克隆 + R2）、`sendMessage`、`messages.type='voice'`。

**关键纪律：**
- 实现前用 worktree 隔离（code 仓多会话高频并发，见 [[feedback_loverush_concurrent_worktree]]）：`git worktree add -b feat/alter-voice-reply ../code-alter-voice origin/main`，在该副本里改，push 后开 PR。`ai_alter.ts` 是最高争用文件，push 前大概率要 rebase。
- `synthesizeWhisper` 用**静态 import**（顶部），不用 `await import()`——动态 import 缺文件会运行时静默降级且 build 不报错（[[loverush_inchat_action_cards]] 教训）。`voice.ts` 已在 origin/main，静态 import 安全。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `apps/api/src/services/ai_alter.ts` | 加常量 + 导出纯函数 `passesVoiceGate` + helper `therapistHasClonedVoice` + 在 `maybeReplyAsAlter` 插语音/文字二分支 | Modify |
| `apps/api/test/unit-voice-gate.test.ts` | `passesVoiceGate` 短句门 + 抽签边界单测 | Create |
| `apps/web/components/VoiceCloneCard.tsx` | `!cloned` 时插催复刻价值主张卡（P0 引导） | Modify |

无 schema 迁移（`messages.type='voice'`、`therapists.eleven_voice_id` 均已存在）。无新接口（复用 `synthesizeWhisper` / `sendMessage` / `GET /voice/me`）。

---

## Task 1: 纯函数 `passesVoiceGate` + 常量（短句门 + 30% 抽签）

**Files:**
- Modify: `apps/api/src/services/ai_alter.ts`（在 `coalesceSegments` 定义附近，约 :770 之后，加常量 + 导出函数）
- Test: `apps/api/test/unit-voice-gate.test.ts`

**说明：** 决策分两半——纯计算部分（长度/URL/30% 抽签）抽成可单测纯函数 `passesVoiceGate`；查库部分（真克隆门）放 Task 2 的 helper。这样大部分逻辑可确定性单测。
分身回复的 `finalText` 是纯对话文本（下单卡/选时段卡是独立 `type` 消息、不在 `finalText` 内），故短句门只需挡「超长」「含链接」，无需排除卡片标记（YAGNI）。

- [ ] **Step 1: 写失败测试**

Create `apps/api/test/unit-voice-gate.test.ts`：

```typescript
/**
 * 单元测试 · passesVoiceGate（分身回复语音化的短句门 + 30% 抽签纯函数部分）
 * 真克隆门（查库）不在此测，靠 Task 3 接入验证覆盖。
 */
import { describe, it, expect } from 'vitest';
import { passesVoiceGate } from '../src/services/ai_alter';

const short = '今晚早点睡，我陪着你。'; // 11 可见字符，<60

describe('passesVoiceGate · 短句门', () => {
  it('空文本 → false', () => {
    expect(passesVoiceGate('', () => 0)).toBe(false);
    expect(passesVoiceGate('   ', () => 0)).toBe(false);
  });

  it('超长(>60 可见字符) → false（即使抽签命中）', () => {
    const longText = '啊'.repeat(61);
    expect(passesVoiceGate(longText, () => 0)).toBe(false);
  });

  it('含 URL → false（即使抽签命中）', () => {
    expect(passesVoiceGate('看这个 https://x.com 哦', () => 0)).toBe(false);
    expect(passesVoiceGate('戳 http://a.cn', () => 0)).toBe(false);
  });

  it('长度按去空白可见字符算（含换行的短多段仍可过长度门）', () => {
    expect(passesVoiceGate('在呢\n\n想你了', () => 0)).toBe(true); // 5 可见字符
  });
});

describe('passesVoiceGate · 30% 抽签边界', () => {
  it('rand < 0.3 → true', () => {
    expect(passesVoiceGate(short, () => 0.29)).toBe(true);
    expect(passesVoiceGate(short, () => 0)).toBe(true);
  });

  it('rand >= 0.3 → false', () => {
    expect(passesVoiceGate(short, () => 0.3)).toBe(false);
    expect(passesVoiceGate(short, () => 0.99)).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/api && npx vitest run test/unit-voice-gate.test.ts`
Expected: FAIL —— `passesVoiceGate` is not exported / not a function。

- [ ] **Step 3: 实现常量 + `passesVoiceGate`**

在 `apps/api/src/services/ai_alter.ts` 的 `coalesceSegments`（约 :770）之后插入：

```typescript
/**
 * 分身回复语音化 · 决策的纯计算部分（短句门 + 30% 抽签）。
 * 真克隆门（查 eleven_voice_id）在 therapistHasClonedVoice，放查库最后一关。
 * 顺序：长度/URL（纯，最便宜）→ 30% 抽签（纯）→ [调用方再查库]，70% 在抽签即挡掉、省 DB。
 * rand 注入便于测试；生产用 Math.random（同 coalesceSegments 范式）。
 */
const VOICE_REPLY_RATIO = 0.3; // 30% 的合格回复转语音
const VOICE_MAX_LEN = 60; // 去空白可见字符上限：长文本不转语音（成本 + 自然，真人语音多短句）
const VOICE_URL_RE = /https?:\/\//i;

export function passesVoiceGate(finalText: string, rand: () => number = Math.random): boolean {
  const text = (finalText ?? '').trim();
  const visibleLen = text.replace(/\s+/g, '').length;
  if (visibleLen === 0 || visibleLen > VOICE_MAX_LEN) return false;
  if (VOICE_URL_RE.test(text)) return false;
  return rand() < VOICE_REPLY_RATIO;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/api && npx vitest run test/unit-voice-gate.test.ts`
Expected: PASS（全部 6 个 it 绿）。

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/services/ai_alter.ts apps/api/test/unit-voice-gate.test.ts
git commit -m "feat(ai_alter): add passesVoiceGate (短句门+30%抽签) for 分身语音回复"
```

---

## Task 2: 真克隆门 helper + 接入 `maybeReplyAsAlter` 语音/文字二分支

**Files:**
- Modify: `apps/api/src/services/ai_alter.ts`
  - 顶部 import 区（约 :48，紧挨 `import { sendMessage, openConversation } from './chat';`）加静态 import
  - `passesVoiceGate` 旁加 helper `therapistHasClonedVoice`
  - 发送段（:1003 防双发检查之后、:1007 `const segments = ...` 之前）插入语音决策 + 改为二分支

**说明：** `therapistHasClonedVoice` 查 `eleven_voice_id` 是否非空——这是「先催复刻再开」的门：没真克隆绝不发语音。语音命中时 `synthesizeWhisper` 用 `cloneOnly:true`，即便 helper 与合成之间有竞态，合成端也只会用真克隆、绝不退 OpenAI 通用女声。

- [ ] **Step 1: 加静态 import**

在 `apps/api/src/services/ai_alter.ts` 第 48 行 `import { sendMessage, openConversation } from './chat';` 下一行加：

```typescript
import { synthesizeWhisper } from './voice';
```

- [ ] **Step 2: 加 `therapistHasClonedVoice` helper**

在 `passesVoiceGate`（Task 1 新增）之后插入：

```typescript
/** 真克隆门：技师是否已用本人声音真克隆（eleven_voice_id 非空）。没克隆 → 不发语音（先催复刻再开）。 */
async function therapistHasClonedVoice(ctx: AiAlterContext, therapistUserId: string): Promise<boolean> {
  const rows = await ctx.db
    .select({ elevenVoiceId: therapists.elevenVoiceId })
    .from(therapists)
    .where(eq(therapists.userId, therapistUserId))
    .limit(1);
  return !!rows[0]?.elevenVoiceId;
}
```

- [ ] **Step 3: 在发送段插入语音决策 + 二分支**

当前 `apps/api/src/services/ai_alter.ts:1005-1042` 是：
```typescript
  // 写入消息(以技师身份发送)· M06 真人式分多条...
  const segments = coalesceSegments(splitIntoSegments(finalText));
  let sent: Awaited<ReturnType<typeof sendMessage>> | undefined;
  for (let i = 0; i < segments.length; i++) {
    // ...首条打字延迟 + 插话检测 + sendMessage(text:seg, isAiAlter:true)...
  }
  if (!sent) return { replied: false, reason: 'empty_segments' };
```

把从 `const segments = ...` 到 `if (!sent) return ...` 整段替换为以下（**文字分支 = 原有循环原封不动；只在外层包一个语音分支**）：

```typescript
  // ───────── 语音化决策（30% 语音 / 70% 文字）─────────
  // 便宜的纯计算门先过(短句+抽签)，命中才查库验真克隆；都过才合成语音。
  // 语音永不阻塞回复：synthesizeWhisper 返 null（无真克隆 / TTS 失败）→ 落回文字分支。
  let voiceAudioUrl: string | null = null;
  if (passesVoiceGate(finalText) && (await therapistHasClonedVoice(ctx, args.therapistUserId))) {
    voiceAudioUrl = await synthesizeWhisper(
      { db: ctx.db },
      { therapistUserId: args.therapistUserId, text: finalText, cloneOnly: true }, // 只用真克隆，绝不退通用女声
    ).catch(() => null);
  }

  let sent: Awaited<ReturnType<typeof sendMessage>> | undefined;

  if (voiceAudioUrl) {
    // ── 语音分支：整条 finalText 作一条 type='voice'，不分段（真人不连发多条语音）──
    // 首条打字感同文字分支：按整条字数估"录制/打字"时长，扣生成已耗时；期间被插话则停。
    const typed = segmentDelayMs(finalText) - (Date.now() - genStartMs);
    if (typed > 0) {
      await sleep(typed);
      if (await hasMessageSince(ctx, args.conversationId, args.customerId, turnStart)) {
        clearTyping();
        return { replied: false, reason: 'interrupted_before_voice' };
      }
      pingTyping();
    }
    sent = await sendMessage(
      { db: ctx.db },
      {
        conversationId: args.conversationId,
        senderUserId: args.therapistUserId,
        // content 存 JSON{text:转录文字, audioUrl:语音}，前端 voice 气泡解析（同 companion voice_whisper）
        text: JSON.stringify({ text: finalText, audioUrl: voiceAudioUrl }),
        type: 'voice',
        isAiAlter: true,
      },
    );
  } else {
    // ── 文字分支：原有多段拟人逻辑，零行为变化 ──
    const segments = coalesceSegments(splitIntoSegments(finalText));
    for (let i = 0; i < segments.length; i++) {
      const seg = segments[i]!;
      if (i === 0) {
        const typed = segmentDelayMs(seg) - (Date.now() - genStartMs);
        if (typed > 0) {
          await sleep(typed);
          if (await hasMessageSince(ctx, args.conversationId, args.customerId, turnStart)) {
            clearTyping();
            return { replied: false, reason: 'interrupted_before_first' };
          }
          pingTyping();
        }
      } else {
        await sleep(segmentDelayMs(seg));
        if (await hasMessageSince(ctx, args.conversationId, args.customerId, turnStart)) {
          clearTyping();
          break;
        }
        pingTyping();
      }
      const s = await sendMessage({ db: ctx.db }, {
        conversationId: args.conversationId,
        senderUserId: args.therapistUserId,
        text: seg,
        isAiAlter: true,
      });
      if (i === 0) sent = s;
    }
  }
  if (!sent) return { replied: false, reason: 'empty_segments' };
```

下游（:1045 起 `aiAlterMessages` 日志用 `sent.id`、`recordSimhash` 用 `finalText`、`touchRelationship`、会话摘要）**保持不变**——语音只是把同一条 `finalText` 换了承载形式。

- [ ] **Step 4: 类型检查 + 单测回归**

Run: `cd apps/api && npx tsc --noEmit && npx vitest run test/unit-voice-gate.test.ts`
Expected: tsc 无错（`synthesizeWhisper`/`therapists`/`eq` 均已可用）；单测仍 PASS。

> 若 `tsc` 报 `@loverush/*` 解析类错误，多为 worktree monorepo 环境噪音（[[feedback_worktree_monorepo_typecheck]]）；认准报错文件是不是 `ai_alter.ts` 本身。

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/services/ai_alter.ts
git commit -m "feat(ai_alter): 分身回复 30% 用技师真克隆声音发语音(cloneOnly)，失败降级文字"
```

---

## Task 3: 接入验证（生产 Mei 端到端 + 无克隆对照）

**说明：** 完整 ai_alter e2e 需 LLM + 大量 fixture、重且脆；本功能最有价值的自动化测试是 Task 1 的纯函数单测，整体接入（真克隆门 + 语音发送 + 降级）按 LoveRush 既有范式用**生产实测**验证（[[loverush_m06_status]]：voice 后端已在生产；Mei 是唯一已真克隆技师 userId `65cd11d4-f3f2-455c-bc09-25b708abc3d4`）。

- [ ] **Step 1: 部署到生产**

push 分支 → 开 PR → 合并到 main（Railway push main 自动部署约 6min，[[loverush_railway_deploy]]）。等部署完成。

- [ ] **Step 2: Mei 对话验证出现语音**

以测试客户登录，向 Mei 连发多条短消息触发分身回复（约 5-10 轮）。预期：
- 出现至少一条语音气泡（`VoiceWhisperBubble`，可点击播放、有转录文字）。
- 其余为文字。语音条不与文字混在同一条、且语音是单条完整（非连发多条语音）。

SQL 佐证（语音消息已落库）：
```bash
railway run -- bash -c 'psql "$DATABASE_URL" -t -c "select type, left(content_original,40) from messages where is_ai_alter=1 and type='\''voice'\'' order by created_at desc limit 5;"'
```
Expected: 有行，`content_original` 是 `{"text":...,"audioUrl":"https://...mp3"}`。

- [ ] **Step 3: 无克隆技师对照（保持纯文字）**

向任一未克隆技师（`eleven_voice_id IS NULL`，当前 66/67）的对话触发回复多轮。预期：**全部文字**，无 voice 消息。SQL：
```bash
railway run -- bash -c 'psql "$DATABASE_URL" -t -c "select count(*) from messages where is_ai_alter=1 and type='\''voice'\'' and sender_user_id <> '\''65cd11d4-f3f2-455c-bc09-25b708abc3d4'\'';"'
```
Expected: 0（除 Mei 外无人发语音）。

- [ ] **Step 4: 短句门验证（含链接/超长走文字）**

在 Mei 对话里诱导一条含链接或较长（>60 字）的回复，确认它是文字而非语音（短句门拦截）。若难诱导，跳过——已由 Task 1 单测覆盖该逻辑。

---

## Task 4: 技师端 P0 催复刻引导卡（未克隆时强化价值主张）

**Files:**
- Modify: `apps/web/components/VoiceCloneCard.tsx`（在状态行 div 之后、`{phase === 'idle' && ...}` 之前插入 `!cloned` 引导卡）

**说明：** 当前未克隆只淡淡显示「还没复刻你的声音」(:257-259)，缺「为什么要复刻」的价值。P0 引导卡点明：复刻后分身约 1/3 用你的声音回复客户、更像真人更留客，催技师去录。`cloned` 变量在 :243 已算好，无需新数据。

- [ ] **Step 1: 插入引导卡**

在 `apps/web/components/VoiceCloneCard.tsx` 第 262 行（状态行那个 `</div>` 之后）、第 264 行 `{/* ── idle:录音 / 选文件 ── */}` 之前插入：

```tsx
        {/* ── 催复刻引导（未克隆时）：点明复刻后分身会用你声音回复客户 ── */}
        {!cloned && (
          <div className="rounded-xl border border-primary/30 bg-gradient-soft p-3">
            <div className="text-[12px] font-semibold leading-5 text-ink-800">
              复刻你的声音，分身回客户时约 1/3 会用你的声音说话
            </div>
            <p className="mt-1 text-[11px] leading-5 text-ink-600">
              语音比纯文字更像真人、更留得住客户。安静环境录 15-30 秒就行，随时能重录替换。
            </p>
          </div>
        )}
```

- [ ] **Step 2: 类型检查 + 构建**

Run: `cd apps/web && npx tsc --noEmit`
Expected: 无错。

- [ ] **Step 3: 视觉确认**

本地起前端（[[loverush_local_qa_stack]]：web 4321）或部署后用 `/browse`，以未克隆技师身份打开 `/t/me/ai-alter`，确认引导卡显示在「声音复刻」区、文案完整、`!cloned` 才出现；以 Mei（已克隆）确认引导卡**不**显示、改显「✅ 已用你本人的声音复刻」。

- [ ] **Step 4: Commit**

```bash
git add apps/web/components/VoiceCloneCard.tsx
git commit -m "feat(web): VoiceCloneCard 未克隆时加催复刻价值引导卡(P0)"
```

---

## Task 5: 上线前置 / 验收清单（非代码）

- [ ] **覆盖核查：** 上线后定期跑，看克隆覆盖是否随引导卡上升：
```bash
railway run -- bash -c 'psql "$DATABASE_URL" -t -c "select count(*) filter (where eleven_voice_id is not null) as cloned, count(*) as total from therapists;"'
```
当前 = 1/67。覆盖率长期低则需运营批量推动技师复刻（引导卡仅被动）。
- [ ] **成本观察：** ElevenLabs 按字符计费。短句门(≤60字)+30% 已限量；若量增，P1 把 `VOICE_REPLY_RATIO`/`VOICE_MAX_LEN` 搬 `app_config`（[[loverush_app_config_miniapp]]）后台可调 + kill switch。
- [ ] **露馅自检：** 确认语音失败时客户端无「合成失败/语音加载失败」字样（静默降级文字）；语音气泡与付费悄悄话同款、无 AI 标识（[[feedback_ai_stealth]]）。

---

## 范围外（YAGNI / P1）

- **主动触达语音化**：`maybeReachOutProactively`（ai_alter.ts ~:1214）也走 sendMessage，本期不动——先验证被动回复的语音体验，再决定主动触达要不要掺语音。
- **比例/阈值后台化 + kill switch**：先用模块常量，验证后再搬 `app_config`。
- **真实波形/时长**：`VoiceWhisperBubble` 现用静态波形 + 占位时长，沿用即可，不在本期。
